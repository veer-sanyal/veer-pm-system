---
description: Study / practice mode for Pillars 2 and 4 (quiz, drill, mock)
argument-hint: [topic or session type]
---

# /tutor $ARGUMENTS

Load `context/study/study-tutor.md` and run tutor mode exactly as it specifies. Do not load it outside study/practice intent; that is what keeps normal sessions lean.

Non-negotiables (the tutor doc is authoritative):

- State lives in `context/study/state.json`; write it after EVERY item, not just at close (a lost session nearly went invisible once).
- Hard gate: Veer states a confidence number before any item is graded.
- Close out per the tutor's protocol and commit state.json.
