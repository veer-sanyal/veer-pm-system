# First LinkedIn post, v6 (2026-06-14)

Reframes v5's middle two paragraphs per Veer's call: drop the "I guessed ~10x and it was wrong" story (saying an obviously wrong guess is wrong proves little) for the stronger, truthful version. The computed figure is 29.4x, which rounds to a clean ~30x, so the honest move is: a round "30x" would have been basically right, and the discipline is not hardcoding it anyway, because a number you'd get right by hand still goes stale on the next data update.

Integrity note: the post's whole credibility rests on "I don't fudge numbers," so the anecdote stays true. The build log records the original placeholder as ~10x (product DECISIONS Entry 009), so v6 does NOT claim "I typed 30x." It claims only what is true: 30x is the round version of the computed 29.4x, and the point is refusing to hardcode even a number that would be right.

Everything else carries over from v5: the orient-before-figures structure, the consulting-over-software surprise, the on-page modeled-assumption caveat, one link in the body.

---

## Post body

There isn't a single hardcoded number on the dashboard I shipped this month. Every figure is computed from the raw data at build time.

First, what it is. Over the past three weeks I built a public dashboard that ranks India's service sectors by how exposed they are to digital-trade policy, things like the WTO e-commerce moratorium. It's built for one user: a trade-policy analyst who, when a policy is on the table, has to decide which sectors are worth a deep dive. Every score links back to the source data behind it.

Now the rule. When I built the chart for India's payments for use of intellectual property, the easy move was to drop in a clean round number, call it 30x since 2005, and move on. I didn't type it. The page computes it from source: 29.4x, $0.67B in 2005 to $19.8B in 2025.

The point isn't that a guess would have been wrong. A "30x" would have been basically right. The point is I'd stopped typing numbers in by hand at all, even ones I'd get right. A hardcoded constant is frozen the day you write it: when the WTO updates the dataset, that "30x" just sits there going stale until someone notices. A figure computed from source is right now and updates itself on the next build. One rule, no maintenance.

That same discipline is why I trust the headline result: the sector most exposed isn't software, India's famous export line. It's consulting and professional services. Not a result I went looking for, just the one the pipeline returned. The one thing the public data genuinely can't support, a firm-level link between a sector's trade and its small firms, is labeled on the page as a modeled assumption rather than buried.

Live walkthrough: https://veer-sanyal.github.io/india-msme-digital-trade-exposure/

For people who build data products: where's your line between a figure you compute live and one you'll hardcode for speed?

---

## Why this version (traceability)

- Change from v5: only the two middle paragraphs. v5 anchored on "the number in my head was ~10x, real is 29.4x," which makes the discipline point by showing a wrong guess. v6 makes the stronger point: even a round "30x" that would have been right gets computed, not typed, because right-today is stale-tomorrow.
- Truthful anchor: 29.4x rounds to ~30x, so "a round 30x would have been basically right" is accurate. The post does not claim Veer typed 30x (the real placeholder was ~10x per DECISIONS Entry 009); claiming otherwise would fabricate an anecdote inside a post about not fabricating numbers.
- Spine unchanged: build discipline (future-proofing, zero stale-constant maintenance), not the error-catch.
- Structure unchanged from v5: hook -> "First, what it is" -> "Now the rule" -> finding + caveat -> link + question.
- Length: ~290 words, the 29.4x figure still above the fold.
- Links: one (live walkthrough); repo stays in the profile Featured/About per the 06-09 decision.

## Posting notes

- **Chart:** pair with `chart_B_ip_licensing_v3.png` (regenerated for v6). It drops the old "~10x guess" line for a dashed "30x" line that lands basically on top of the computed 2025 value, matching the post's framing. Generated by `make_chart_B.py`.
- Do not reuse chart_B_ip_licensing_v2.png (carries the "~10x would be wrong" framing this version rejects).
- Alternative with zero rework: Chart A (consulting) still works and illustrates the consulting finding instead of the 30x example.
- Post when you have 60-90 min to reply; seed early comments substantively (golden hour).
- Hashtags optional, 3-5 if used: #productmanagement #buildinpublic #datavisualization
- Supersedes v2, v3, v4, v5.
